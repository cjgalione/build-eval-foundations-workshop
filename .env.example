# Secrets only — loaded by the Python agent / Streamlit / evals via load_dotenv().
# Everything else (BRAINTRUST_PROFILE, BRAINTRUST_DEFAULT_PROJECT) is a session export
# — see WORKSHOP.md §4.2.
OPENAI_API_KEY=
BRAINTRUST_API_KEY=
